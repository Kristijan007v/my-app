/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            'https://foto-zeljko-dev.b-cdn.net',
        ],
    },
}

module.exports = nextConfig
